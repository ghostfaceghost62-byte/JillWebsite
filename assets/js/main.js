(() => { const key='bookreserve-theme', saved=localStorage.getItem(key); if(saved==='dark'||(!saved&&matchMedia('(prefers-color-scheme: dark)').matches))document.body.classList.add('dark'); document.querySelector('[data-theme]')?.addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem(key,document.body.classList.contains('dark')?'dark':'light')}); document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',()=>{const b=f.querySelector('button[type=submit]');if(b){b.disabled=true;b.dataset.label=b.textContent;b.textContent='Processing…'}}));})();
